# Currently a do-nothing script

# Draft a release on Github then...

#git tag v0.3.0
#git push --tags
#ssh root@frenzy.joeldare.com
#cd /opt/frenzy
#docker-compose up --build --detach nolific

# One-line based on above
# ssh root@frenzy.joeldare.com -t 'cd /opt/frenzy && docker-compose up --build --detach nolific'
